{
  "name": "automation-clear-scheduled-run-limit-patch",
  "group": "A",
  "description": "An update request clearing the scheduled-run cap carries a clear operation on scheduledRunLimit, distinct from an omitted field and from a set operation.",
  "type": "StateAction",
  "input": {
    "type": "automation/updateRequested",
    "resource": "ahp-automation:/capped",
    "changes": {
      "scheduledRunLimit": { "kind": "clear" }
    }
  },
  "acceptableOutputs": [{
    "type": "automation/updateRequested",
    "resource": "ahp-automation:/capped",
    "changes": {
      "scheduledRunLimit": { "kind": "clear" }
    }
  }]
}
